diff --git a/.gitignore b/.gitignore
index d247489..55e1cbd 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1,2 +1,3 @@
 easerve/
 .env*
+ssl/
diff --git a/nginx/Dockerfile b/nginx/Dockerfile
index baee044..bb0f8ed 100644
--- a/nginx/Dockerfile
+++ b/nginx/Dockerfile
@@ -4,12 +4,14 @@ FROM alpine:3.21
 
 RUN apk update && \
     apk add --no-cache nginx && \
-    mkdir -p /etc/nginx/ssl
+    mkdir -p /etc/nginx/ssl /var/www/nginx/.well-known
 
 # Copy the configuration file
 COPY conf/default.conf /etc/nginx/http.d/default.conf
 COPY ssl/origin.pem /etc/nginx/ssl/origin.pem
 COPY ssl/origin.key /etc/nginx/ssl/origin.key
+# FIXME: Jab Training
+COPY assetlinks.json /var/www/nginx/.well-known/assetlinks.json
 
 # Port to expose, Just documentation(Not necessary)
 EXPOSE 443/tcp
diff --git a/nginx/conf/default.conf b/nginx/conf/default.conf
index e8842ac..a13ed4b 100644
--- a/nginx/conf/default.conf
+++ b/nginx/conf/default.conf
@@ -18,6 +18,10 @@ server {
   ssl_protocols TLSv1.2 TLSv1.3;
   ssl_ciphers HIGH:!aNULL:!MD5; # HIGH: 모든 암호화 알고리즘을 사용하되, aNULL과 MD5는 사용하지 않는다.
 
+  location /.well-known/ {
+    root /var/www/nginx;
+  }
+
   location / {
     proxy_pass http://easerve:3000;
     proxy_http_version 1.1;
